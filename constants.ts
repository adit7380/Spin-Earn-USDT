// Conversion rate - coins to USDT
export const COIN_TO_USDT_RATE = 50000; // 50,000 coins = 1 USDT

// Minimum withdrawal amount in USDT
export const MIN_WITHDRAWAL_USDT = 10;

// Spin wheel segments (rewards)
export const SPIN_REWARDS = [2, 4, 6, 8, 10, 15, 20];

// Daily spin limit
export const DAILY_SPIN_LIMIT = 100;

// Spin cooldown in seconds
export const SPIN_COOLDOWN_SECONDS = 3600; // 1 hour

// Referral bonus (coins)
export const REFERRAL_BONUS = 50;

// Referral earnings percentage
export const REFERRAL_EARNINGS_PERCENTAGE = 0.05; // 5%
